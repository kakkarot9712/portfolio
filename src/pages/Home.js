import React from "react";
import Intro from "../components/Home/Intro";

const Home = (props) => {
  return (
    <React.Fragment>
      <Intro />
    </React.Fragment>
  );
};

export default Home;
